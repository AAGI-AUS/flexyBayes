% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_inla.R
\name{confint.flexybayes_inla}
\alias{confint.flexybayes_inla}
\title{Credible intervals for the fixed effects of a per-row INLA fit}
\usage{
\method{confint}{flexybayes_inla}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{flexybayes_inla} fit carrying fixed-effect marginals.}

\item{parm}{Character vector of coefficient names to return, or \code{NULL}
(the default) for every fixed effect.}

\item{level}{Credible level for the interval, as a proportion. The
default \code{0.95} returns the 2.5th and 97.5th posterior percentiles.}

\item{...}{Ignored, present for compatibility with the generic.}
}
\value{
A matrix with one row per fixed effect and two columns holding
the lower and upper credible bounds, named for the percentiles used.
}
\description{
Quantiles of INLA's own posterior marginals for the fixed effects, not
frequentist confidence intervals and not a normal approximation to
them. The bounds come from \code{INLA::inla.qmarginal()} applied to the
marginal densities the fit stores, so any credible level is available
rather than only the 0.95 that \code{summary.fixed} tabulates.
}
\details{
Before 0.9.0 an INLA fit did not inherit the \code{flexybayes} parent class,
so \code{confint()} on one reached \code{stats::confint.default} and failed on a
missing \code{vcov} contract. This method is the INLA sibling of
\code{\link[=confint.flexybayes]{confint.flexybayes()}}.
}
