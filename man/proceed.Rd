% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{proceed}
\alias{proceed}
\alias{proceed.flexybayes_review}
\title{Advance a deferred-execution object into its fit}
\usage{
proceed(x, ...)

\method{proceed}{flexybayes_review}(x, ...)
}
\arguments{
\item{x}{A \verb{<flexybayes_review>} object holding the deferred fit.}

\item{...}{Reserved for future deferred-execution classes, such as a
deferred triangulation.}
}
\value{
The fit object the originating call would have returned, of
class \code{flexybayes}. A second call returns the cached fit.
}
\description{
Generic for the inspect-then-fit pattern. The
\verb{<flexybayes_review>} method restores the RNG snapshot
captured at review-object construction, runs the deferred fit
via the backend driver, caches the result in-place, and
returns the fit. A second call returns the cached fit.
}
\examples{
# The review object holds the model back until proceed() is called.
set.seed(1)
d <- data.frame(y = rnorm(60), x = rnorm(60), g = factor(rep(1:6, 10)))
rv <- flexybayes(y ~ x + (1 | g), data = d, backend = "brms",
                 review_code = TRUE)
rv
\donttest{
if (requireNamespace("brms", quietly = TRUE)) {
  fit <- proceed(rv)
  class(fit)
}
}
}
