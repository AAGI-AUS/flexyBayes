% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_log_posterior.R
\name{fb_log_posterior}
\alias{fb_log_posterior}
\alias{fb_log_posterior.default}
\alias{fb_log_posterior.flexybayes_brms}
\alias{fb_log_posterior.flexybayes_inla}
\title{Emit a flexyBayes posterior as a log-density producer}
\usage{
fb_log_posterior(fit, ...)

\method{fb_log_posterior}{default}(fit, ...)

\method{fb_log_posterior}{flexybayes_brms}(fit, ...)

\method{fb_log_posterior}{flexybayes_inla}(fit, ...)
}
\arguments{
\item{fit}{A fitted flexyBayes object. Every current backend class
abstains; the generic and its methods are retained so a consumer can
dispatch on the abstention rather than discovering the gap by a
missing method.}

\item{...}{Reserved for future producer options; currently unused.}
}
\value{
Does not return on any current backend: it raises a classed
\code{fb_c4_unavailable} condition naming the backend and the reason.
}
\description{
Turns a fitted flexyBayes object into a log-posterior producer:
a vectorised, domain-safe, unnormalised log-posterior callable that
\code{proxymix::from_fb_posterior()} compresses into a closed-form
Gaussian-mixture proxy. It is the single inference-result outflow from
flexyBayes; the contract is the \emph{log-density}, not the draws, so the
returned object is addressed purely through its callable.
}
\details{
The returned value is a \strong{bare callable}
\verb{function(theta_matrix) -> numeric}. Its input is a numeric matrix whose
rows index independent parameter draws and whose columns index
parameters, in \code{attr(., "parameter_names")} order, on the natural
(constrained) scale. Its output is a length-\code{nrow(theta_matrix)} numeric
vector of \verb{log p(theta | data) + const} (unnormalised). The callable is
vectorised, side-effect free, and domain-safe: a row outside the
parameters' support returns \code{-Inf} rather than raising an error (the
consumer probes it at construction). It carries, as attributes:

\describe{
\item{\code{parameter_names}}{Character vector naming the parameters; its
length fixes the proxy's ambient dimension. Vector-valued targets
are flattened in column-major order with index suffixes (e.g.
\code{beta[1,1]}, \code{beta[2,1]}).}
\item{\code{log_normalizer}}{The additive correction that would normalise
the density, i.e. \verb{-log Z}. For a posterior the marginal likelihood
is generally unknown, so this is \code{NA_real_} and the consumer
reports a shifted (not absolute) divergence.}
\item{\code{support_lower}, \code{support_upper}}{Length-\code{n_dim} numeric support
bounds taken from the model's parameter constraints (\code{NA} for an
unbounded coordinate). A variance / scale parameter is bounded below
by zero, for instance. Used only to centre and scale the consumer's
default importance proposal.}
\item{\code{draws}}{An \code{n} by \code{n_dim} numeric matrix of the fit's posterior
draws on the natural scale, column-aligned to \code{parameter_names}.
Used only to seed the consumer's default proposal; never required.}
}

Backend support. No active backend produces a real C4 log-density today.
The \strong{brms} and \strong{INLA} backends both abstain with an informative
condition -- brms's log-density lives on the Stan unconstrained scale
with a version-fragile name mapping, and INLA's posterior is a
deterministic approximation, not a sampling log-density. Abstaining is
preferred to emitting a plausible-but-wrong log-density.

Acyclic note. A consumer such as proxymix uses this callable without
depending on flexyBayes; flexyBayes does not list proxymix in \code{Imports}
or \code{Suggests}. The cross-package demonstration lives in a separate
integration harness, not in this package, preserving the acyclic
dependency graph.
}
\section{Lifecycle}{

No active backend produces a real C4 log-density; both \code{flexybayes_brms}
and \code{flexybayes_inla} fits abstain with the classed condition
\code{fb_c4_unavailable}, for the reasons given above. The generic and its
methods are retained so a consumer can dispatch on the abstention rather
than discovering the gap by a missing method.
}

\seealso{
\code{proxymix::from_fb_posterior()} for the consumer (compresses
the returned callable into a Gaussian-mixture proxy).
}
\concept{flexyBayes interop}
