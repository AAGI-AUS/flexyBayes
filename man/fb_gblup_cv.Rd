% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_gblup_cv.R
\name{fb_gblup_cv}
\alias{fb_gblup_cv}
\title{Genomic-prediction accuracy by cross-validation}
\usage{
fb_gblup_cv(y, K, X = NULL, folds = 5L, repeats = 1L, seed = NULL, tol = 1e-08)
}
\arguments{
\item{y}{Numeric vector of phenotypes, one per genotype, aligned to the
rows of \code{K}.}

\item{K}{An \verb{n x n} genomic (or pedigree) relationship matrix.}

\item{X}{Optional \verb{n x p} fixed-effect design matrix (an intercept is
used when \code{NULL}).}

\item{folds}{Number of cross-validation folds (default 5).}

\item{repeats}{Number of independent fold partitions to average over
(default 1); repeated CV stabilises the estimate.}

\item{seed}{Optional integer seed for reproducible fold assignment.}

\item{tol}{PSD / symmetry tolerance for the spectral decomposition.}
}
\value{
An \code{fb_gblup_cv} object: \code{accuracy} (pooled predicted-observed
correlation), \code{accuracy_per_fold}, \code{bias} (pooled observed-on-
predicted slope), \code{predicted} (out-of-fold predictions in input
order, averaged over repeats), \code{h2} (REML heritability on the full
data), and metadata.
}
\description{
Estimate how accurately a genomic BLUP predicts the performance of
genotypes whose phenotypes are held out. For each fold the variance
components are estimated by REML on the training genotypes, the held-out
breeding values are predicted from the genomic relationship matrix
(\eqn{\hat{u}_V = K_{VT}(K_{TT} + \delta I)^{-1}(y_T - X_T\hat\beta)}),
and the predictions are compared with the observed phenotypes. The
headline metric is \emph{prediction accuracy}, the correlation between
predicted and observed across the held-out genotypes; \emph{bias} is the
slope of observed on predicted (1 is unbiased).
}
\examples{
\donttest{
set.seed(1)
n <- 80L; m <- 400L
Z <- matrix(rbinom(n * m, 2L, 0.3), n, m)
Zc <- scale(Z, scale = FALSE)
G <- tcrossprod(Zc) / m + diag(n) * 1e-3
u <- as.vector(t(chol(G)) \%*\% rnorm(n))
y <- u + rnorm(n)
cv <- fb_gblup_cv(y, G, folds = 5L, seed = 1L)
cv$accuracy
}
}
