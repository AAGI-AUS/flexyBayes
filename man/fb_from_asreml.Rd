% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb.R
\name{fb_from_asreml}
\alias{fb_from_asreml}
\title{Ingest an ASReml-format model specification into the flexyBayes IR}
\usage{
fb_from_asreml(
  fixed,
  random = NULL,
  rcov = NULL,
  data,
  family = "gaussian",
  link = NULL,
  weights = NULL,
  known_matrices = list(),
  prior = NULL,
  prior_fixed_sd = 100,
  prior_vc_sd = 1
)
}
\arguments{
\item{fixed}{Two-sided formula: \code{response ~ fixed_effects}.}

\item{random}{One-sided formula \code{~ random_terms} (ASReml syntax), or
\code{NULL}.}

\item{rcov}{One-sided formula \code{~ residual_structure}, or \code{NULL}. \code{NULL}
defaults to iid residuals (\code{list(list(type = "units"))}), matching
\code{\link[=flexybayes]{flexybayes()}}.}

\item{data}{A data.frame containing every variable referenced.}

\item{family}{Character family name (\code{gaussian}, \code{binomial}, \code{binary},
\code{poisson}, \code{negative_binomial}, \code{negbinom}, \code{gamma}, \code{beta}).}

\item{link}{Character link override, or \code{NULL} for the family default.}

\item{weights}{Optional numeric vector of length \code{nrow(data)}. When
non-\code{NULL}, mapped to a single addition term with \code{type = "weights"}.}

\item{known_matrices}{Named list of known matrices (e.g.
\code{list(Gmat = G)}); names are recorded in
\code{data_summary$known_matrices}.}

\item{prior}{An \code{\link[=fb_prior]{fb_prior()}} object, or \code{NULL}. \code{NULL} falls back to
the scalar priors \code{prior_fixed_sd} / \code{prior_vc_sd}.}

\item{prior_fixed_sd}{Numeric SD for fixed-effect normal priors when
\code{prior} is \code{NULL}.}

\item{prior_vc_sd}{Numeric hyperparameter for the variance-component
priors when \code{prior} is \code{NULL}.}
}
\value{
An \code{fb_terms} object with \code{source = "asreml"}.
}
\description{
Parses an ASReml-style \code{fixed} / \code{random} / \code{rcov} specification into
a \code{fb_terms} object -- flexyBayes's backend-agnostic intermediate
representation (IR) of a model. The IR is what every engine emits
from, so building it explicitly lets a power user inspect the parsed
model, cache it, or hand it to a fitting verb. Most users never call
this directly: \code{\link[=flexybayes]{flexybayes()}} and \code{\link[=fb]{fb()}} build the IR internally from
the same arguments. Argument names and defaults mirror \code{\link[=flexybayes]{flexybayes()}}
one-for-one, so this is a drop-in for that function's parsing step.
}
\examples{
df <- data.frame(
  yield = rnorm(20),
  geno  = factor(rep(letters[1:4], 5)),
  env   = factor(rep(c("a", "b"), 10))
)
ir <- fb_from_asreml(yield ~ env, random = ~ geno, data = df)
class(ir)
}
\seealso{
\code{\link[=flexybayes]{flexybayes()}} and \code{\link[=fb]{fb()}} for the universal fitting entry;
\code{\link[=fb_from_brms]{fb_from_brms()}} and \code{\link[=fb_from_greta]{fb_from_greta()}} for the other ingest dialects.

Other flexyBayes ingest adapters: 
\code{\link{fb_from_brms}()},
\code{\link{fb_from_greta}()}
}
\concept{flexyBayes ingest adapters}
