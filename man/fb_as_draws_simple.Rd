% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate.R
\name{fb_as_draws_simple}
\alias{fb_as_draws_simple}
\alias{fb_as_draws_simple.flexybayes_inla}
\alias{fb_as_draws_simple.flexybayes_brms}
\alias{fb_as_draws_simple.default}
\title{Extract per-parameter posterior draws from a model fit}
\usage{
fb_as_draws_simple(fit, ...)

\method{fb_as_draws_simple}{flexybayes_inla}(fit, n_samples = 1000L, ...)

\method{fb_as_draws_simple}{flexybayes_brms}(fit, ...)

\method{fb_as_draws_simple}{default}(fit, ...)
}
\arguments{
\item{fit}{A model fit object carrying a posterior, dispatched on by
the methods listed above.}

\item{...}{Method-specific arguments, such as \code{n_samples} for the
INLA method.}
}
\value{
A named list of numeric vectors, one element per parameter,
each element holding that parameter's posterior draws.
}
\description{
S3 generic used by \code{triangulate()} to extract a named list of
numeric posterior-draw vectors from each fit. The methods that reach
an active engine are \code{flexybayes_brms} and \code{flexybayes_inla}.
User-defined methods can extend the generic.
}
\examples{
# A named list of draw vectors, with no posterior dependency.
\donttest{
if (requireNamespace("INLA", quietly = TRUE)) {
  set.seed(1)
  d <- data.frame(y = rnorm(60), x = rnorm(60), g = factor(rep(1:6, 10)))
  fit <- flexybayes(y ~ x + (1 | g), data = d, backend = "inla",
                    verbose = FALSE)
  dr <- fb_as_draws_simple(fit)
  names(dr)
}
}
}
\keyword{internal}
