% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{met_example}
\alias{met_example}
\title{Example Multi-Environment Trial (MET) Dataset}
\format{
A list with the following components:
\describe{
\item{\code{dat}}{A data frame with 120 rows and the following columns:
\describe{
\item{\code{geno}}{Factor: genotype identifier (G1--G10)}
\item{\code{env}}{Factor: environment identifier (E1--E4)}
\item{\code{rep}}{Factor: replicate (R1--R3)}
\item{\code{block}}{Factor: incomplete block within rep (B1--B6)}
\item{\code{row}}{Factor: row position in field layout}
\item{\code{col}}{Factor: column position in field layout}
\item{\code{yield}}{Numeric: grain yield (continuous, primary response)}
\item{\code{x_cov}}{Numeric: a continuous covariate}
\item{\code{bin_y}}{Integer: binary response (0/1)}
\item{\code{count_y}}{Integer: count response (Poisson-like)}
}
}
\item{\code{G_mat}}{10x10 genomic relationship matrix (G = MM'/2pq)}
\item{\code{A_mat}}{10x10 pedigree-based relationship matrix}
\item{\code{M_geno}}{10x50 marker matrix (50 SNP markers for 10 genotypes)}
\item{\code{n_geno}}{Integer: number of genotypes (10)}
\item{\code{n_env}}{Integer: number of environments (4)}
}
}
\source{
Generated synthetically for package demonstration purposes.
}
\usage{
met_example
}
\description{
A synthetic multi-environment trial dataset suitable for demonstrating
mixed model formulations. Contains yield data for 10 genotypes evaluated
across 4 environments with 3 replicates per environment, plus spatial
layout, genomic markers, and multiple response types.
}
\details{
The dataset is generated with known variance components:
\itemize{
\item Genotype variance: sigma_g^2 = 4
\item Environment variance: sigma_e^2 = 9 (varies by environment)
\item GxE variance: sigma_ge^2 = 2
\item Residual variance: sigma^2 = 1
}

The genomic relationship matrix is computed from 50 simulated SNP markers
using the VanRaden method 1. The pedigree matrix uses a simple half-sib
structure.
}
\examples{
data(met_example)
str(met_example$dat)
head(met_example$dat)

# Simple model (small budget for example purposes)
\dontrun{
# live fit -- needs a backend (greta Python/TF, INLA, or brms/Stan)
fit <- flexybayes(
  fixed  = yield ~ env,
  random = ~ geno,
  data   = met_example$dat,
  n_samples = 100, warmup = 100, chains = 1, verbose = FALSE
)
summary(fit)
}

}
\keyword{datasets}
