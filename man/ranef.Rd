% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{ranef}
\alias{ranef}
\alias{ranef.flexybayes}
\title{Random-effect predictions from a flexyBayes fit}
\usage{
ranef(object, ...)

\method{ranef}{flexybayes}(object, ...)
}
\arguments{
\item{object}{A fitted \code{flexybayes} object of any backend.}

\item{...}{Ignored, present for compatibility with the generic.}
}
\value{
A named list of data frames, one per grouping factor. Empty
when the model carries no random terms.
}
\description{
The random-effect table, one data frame per grouping factor, in the
same columns on every engine: \code{group}, \code{level}, \code{estimate},
\code{std.error}, \code{conf.low}, \code{conf.high}. Identical to
\code{coef(object, what = "random")}, and present under this name because
it is the name the mixed-model ecosystem uses.
}
\details{
The method is registered on both this package's own \code{ranef()} generic
and on \pkg{nlme}'s, which is the generic \pkg{lme4} and \pkg{brms}
re-export. Attaching any of those masks the local generic, and the
registration on nlme's is what keeps a bare \code{ranef(fit)} dispatching
there. In a session where the masking is ambiguous, call
\code{flexyBayes::ranef(fit)}.
}
\seealso{
\code{\link[=coef.flexybayes]{coef.flexybayes()}}, which this delegates to.
}
