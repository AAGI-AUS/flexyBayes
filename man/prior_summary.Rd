% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_summary.R
\name{prior_summary}
\alias{prior_summary}
\alias{prior_summary.flexybayes_inla}
\alias{prior_summary.flexybayes_brms}
\alias{prior_summary.default}
\title{Resolved-prior summary for a flexyBayes fit}
\usage{
prior_summary(object, ...)

\method{prior_summary}{flexybayes_inla}(object, ...)

\method{prior_summary}{flexybayes_brms}(object, ...)

\method{prior_summary}{default}(object, ...)
}
\arguments{
\item{object}{A \code{flexybayes_inla} or \code{flexybayes_brms} object.}

\item{...}{Ignored by current methods (reserved for future
per-component selection).}
}
\value{
A \code{prior_summary_flexybayes} object, a list carrying the
components below.

\describe{
\item{\code{kind}}{One of \code{"fb_prior"}, \code{"legacy_scalar"},
\code{"no_prior_recorded"}.}
\item{\code{backend}}{The backend the fit ran on: \code{"inla"} or
\code{"brms"}.}
\item{\code{fb_prior}}{The \code{fb_prior} object (when
\code{kind == "fb_prior"}).}
\item{\code{default_origin}}{\code{"auto"} when the prior was
constructed by the bounded-uniform auto-default;
\code{"user"} when supplied via the \code{prior} argument; \code{NA} for
legacy / no-prior cases.}
\item{\code{default_scale}, \code{default_basis}}{Attributes carried by
the auto-default prior naming the response-scale upper
bound and its basis (response-scale \code{sd(y)}, logit-scale
constant, log-scale constant). \code{NULL} when the prior was
user-supplied.}
\item{\code{fixed_sd}, \code{vc_sd}}{Legacy scalar values (when
\code{kind == "legacy_scalar"}).}
\item{\code{scalars_supplied}}{Named logical: whether the caller
supplied \code{prior_fixed_sd} and \code{prior_vc_sd}. An argument left
unsupplied leaves the engine's own default in force, which
\code{fixed_sd_engine_default} names.}
\item{\code{legacy_vc_applied}}{Named character: the density the
legacy scalar bridge put on each variance component it reached,
empty when \code{prior_vc_sd} was not supplied.}
\item{\code{not_applied}}{Named character: a prior this package
declared that the engine has no parameter for -- a residual
prior on a family whose dispersion is a function of the mean.}
\item{\code{declaration_only}}{Reserved; always \code{FALSE} for every
current backend.}
}
}
\description{
Returns the resolved priors used to fit the model -- either the
user-supplied \code{fb_prior()} object, the auto-default bounded
uniform on the standard-deviation scale (Gelman, 2006), or the
legacy scalar bridge (\code{prior_fixed_sd} + \code{prior_vc_sd}). The
return value is an S3 object with a \code{print()} method; the
underlying \code{fb_prior} (when applicable) is exposed under
\verb{$fb_prior} for programmatic access.
}
\examples{
\dontrun{
# live brms (Stan) fit -- needs a working Stan toolchain
data(sleepstudy, package = "lme4")
fit <- fb_brms(Reaction ~ Days + (1 | Subject),
               data = sleepstudy,
               n_samples = 100, warmup = 100, chains = 1,
               verbose = FALSE, mcmc_verbose = FALSE)
prior_summary(fit)
}

}
