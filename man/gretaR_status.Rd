% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gretaR_slot.R
\name{gretaR_status}
\alias{gretaR_status}
\title{Introspect the gretaR backend slot}
\usage{
gretaR_status()
}
\value{
A list with components:
\describe{
\item{\code{activated}}{Logical: \code{TRUE} if
\code{options(flexyBayes.gretaR_activated)} is set AND a future
audit mechanism has cleared. v0.2: always \code{FALSE}.}
\item{\code{gretaR_installed}}{Logical: result of
\code{nzchar(system.file(package = "gretaR"))}.}
\item{\code{audit_clean}}{Logical or \code{NA}: audit-status indicator.
v0.2: always \code{NA} (no audit-status mechanism shipped).}
\item{\code{dormancy_reason}}{Character: one of
\code{"slot_provisioned_not_activated"}, \code{"gretaR_not_installed"},
\code{"gretaR_not_audit_clean"}, or \code{"gretaR_dispatch_eligible"}
(the last meaning the slot is fully active).}
\item{\code{activation_procedure}}{Character vector: numbered steps
to activate the slot, in order.}
}
}
\description{
Returns the activation state of the gretaR backend slot plus the
procedure to activate it. The gretaR slot is provisioned at v0.2
but not yet a live backend; activation lands in v0.3 when the
gretaR package is publicly available and has cleared the audit
gate. Until then, this helper is the canonical discoverability
surface --- users who see "gretaR" referenced in the backend
matrix or release notes can call \code{gretaR_status()} to inspect why
the slot is dormant and how to wake it.
}
\examples{
gs <- gretaR_status()
gs$activated         # v0.2: FALSE
gs$dormancy_reason   # v0.2: "slot_provisioned_not_activated" or
                     #       "gretaR_not_installed"
cat(gs$activation_procedure, sep = "\n")

}
