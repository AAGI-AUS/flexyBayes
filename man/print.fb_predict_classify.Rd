% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_classify.R
\name{print.fb_predict_classify}
\alias{print.fb_predict_classify}
\title{Print a classify means table}
\usage{
\method{print}{fb_predict_classify}(x, digits = 4L, ...)
}
\arguments{
\item{x}{An \code{fb_predict_classify} table, as returned by
\code{predict(fit, classify = )}.}

\item{digits}{Number of significant digits for the printed table.}

\item{...}{Ignored, present for compatibility with the generic.}
}
\value{
Invisibly, \code{x} unchanged. Called for the table it prints.
}
\description{
Prints the table with a banner naming what the numbers are. The
estimate is a posterior mean of a marginal mean, and the table is not
a standard-error-of-difference table -- ASReml's \code{predict()} prints
one beside the means and this does not, so the banner says so rather
than leaving the absence to be noticed.
}
\details{
On an INLA fit the banner carries a second line. The interval there
comes from the Gaussian approximation of the joint fixed-effect
posterior, which is what the estimation basis supplies to emmeans, and
not from INLA's own marginal densities.
}
