% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_from_greta.R
\name{fb_from_greta}
\alias{fb_from_greta}
\title{Ingest a user-built greta model into the flexyBayes IR}
\usage{
fb_from_greta(
  model,
  data = NULL,
  prior = NULL,
  canonical_names = NULL,
  known_matrices = list()
)
}
\arguments{
\item{model}{A \code{greta_model} returned by \code{greta::model(...)}.}

\item{data}{Optional data.frame used to build the model, recorded on
the IR for downstream methods. Not required for fitting -- greta has
already captured the data into its TensorFlow graph.}

\item{prior}{Optional \code{\link[=fb_prior]{fb_prior()}} object. When supplied, every name
must resolve to a target greta_array; semantic agreement with the
graph's encoded priors is the caller's responsibility.}

\item{canonical_names}{Optional named character vector mapping
greta-side parameter names to canonical names. \code{NULL} falls back to
the verbatim greta names (with a one-time silenceable note, since
\code{\link[=triangulate]{triangulate()}} against a non-greta backend needs canonical names).}

\item{known_matrices}{Named list, mirroring the ASReml entry; recorded
in \code{data_summary$known_matrices}.}
}
\value{
An \code{fb_terms} object with \code{source = "greta"}, \code{intercept = NA},
empty fixed / random / rcov term lists, and the populated
\code{greta_meta} slot (carrying the model graph, target arrays, and the
canonical-name map). Pass the returned IR straight to \code{\link[=fb]{fb()}} to fit
the graph via greta while keeping a canonical-name map for
\code{\link[=triangulate]{triangulate()}}: \code{fb(fb_from_greta(model, canonical_names = ...))}.
}
\description{
Wraps a \code{greta_model} (built with \code{greta::model(...)}) in a \code{fb_terms}
object -- flexyBayes's backend-agnostic intermediate representation
(IR) -- so a natively-specified greta graph can flow through the same
downstream machinery (summaries, \code{\link[=triangulate]{triangulate()}}, canonical-name
mapping) as a formula-ingested model. Unlike the formula adapters this
is a post-hoc wrapper around an already-built graph, so the resulting
IR is greta-only by construction: it carries no fixed / random / rcov
term lists, only the populated \code{greta_meta} slot.
}
\examples{
\dontrun{
library(greta)
mu    <- normal(0, 10)
sigma <- normal(0, 5, truncation = c(0, Inf))
y     <- as_data(rnorm(20))
distribution(y) <- normal(mu, sigma)
m  <- model(mu, sigma)
ir <- fb_from_greta(m, canonical_names = c(mu = "(Intercept)"))
fit <- fb(ir)   # fit the native graph via greta, keeping the map
}
}
\seealso{
\code{\link[=fb]{fb()}} and \code{\link[=flexybayes]{flexybayes()}} for the universal fitting entry
(which accept the returned IR directly); \code{\link[=fb_from_asreml]{fb_from_asreml()}} and
\code{\link[=fb_from_brms]{fb_from_brms()}} for the formula dialects.

Other flexyBayes ingest adapters: 
\code{\link{fb_from_asreml}()},
\code{\link{fb_from_brms}()}
}
\concept{flexyBayes ingest adapters}
