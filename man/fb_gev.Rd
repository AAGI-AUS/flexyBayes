% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_extreme_value.R
\name{fb_gev}
\alias{fb_gev}
\title{Fit a generalised extreme value distribution to block maxima}
\usage{
fb_gev(y, return_periods = c(10, 50, 100), conf_level = 0.95)
}
\arguments{
\item{y}{Numeric vector of block-maxima observations. Must contain at
least four finite values.}

\item{return_periods}{Numeric vector of return periods (in blocks) at which
to report return levels. Defaults to \code{c(10, 50, 100)}.}

\item{conf_level}{Numeric in \verb{(0, 1)}. The interval level for the parameter
summary. Defaults to \code{0.95}.}
}
\value{
An object of class \code{c("fb_gev_fit", "fb_family_fit")}: a list with
\code{estimates} (a \code{data.frame} of \code{term} / \code{estimate} / \code{std.error} /
\code{conf.low} / \code{conf.high}), \code{return_levels} (a \code{data.frame} of
\code{return_period} / \code{return_level}), \code{method}, \code{n_obs}, and \code{logLik}.
}
\description{
Estimates the location, scale, and shape of a GEV distribution from a
vector of block-maxima observations by maximum likelihood (base \code{optim()},
dependency-free and deterministic given the data).
}
\details{
Maximum likelihood maximises the GEV log-likelihood directly, with the
scale optimised on the log scale to keep it positive and the standard
errors recovered from the observed-information (Hessian) matrix by the
delta method on the scale.

Return levels (the level exceeded once per \code{m} blocks on average) are
computed from the fitted parameters and reported on \code{fit$return_levels}
for the requested return periods.

A scalable Bayesian GEV belongs on INLA's native \code{gev} / \code{bgev} family
and is planned for a future release; the greta backend ships no GEV
distribution.
}
\examples{
set.seed(1)
y <- rgev(200L, location = 10, scale = 2, shape = 0.1)
fit <- fb_gev(y)
fit$estimates
}
\seealso{
\code{\link[=fb_family_gev]{fb_family_gev()}}, \code{\link[=rgev]{rgev()}}, \code{\link[=tidy.fb_gev_fit]{tidy.fb_gev_fit()}}
}
