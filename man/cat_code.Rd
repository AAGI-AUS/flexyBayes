% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/review.R
\name{cat_code}
\alias{cat_code}
\alias{cat_code.flexybayes_review}
\title{Emit the generated backend code for a deferred review object}
\usage{
cat_code(x, ...)

\method{cat_code}{flexybayes_review}(x, file = stdout(), ...)
}
\arguments{
\item{x}{A \verb{<flexybayes_review>} object carrying the generated
backend code.}

\item{...}{Method-specific arguments. The \code{flexybayes_review} method
accepts \code{file}, a connection defaulting to \code{stdout()}.}

\item{file}{A connection to write the code to, defaulting to
\code{stdout()}.}
}
\value{
Invisibly, the code as a character vector. Called for the
code it writes to \code{file}.
}
\description{
Writes the backend code carried by a \verb{<flexybayes_review>}
object (Stan code via \code{\link[brms:stancode]{brms::stancode()}} for
\code{\link[=flexybayes]{flexybayes()}} / \code{fb_brms()}) to a connection.
}
\examples{
# review_code = TRUE builds the engine code without fitting, so this
# runs without INLA or brms installed.
set.seed(1)
d <- data.frame(y = rnorm(60), x = rnorm(60), g = factor(rep(1:6, 10)))
rv <- flexybayes(y ~ x + (1 | g), data = d, backend = "brms",
                 review_code = TRUE)
cat_code(rv)
}
