% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refusal_taxonomy.R
\name{fb_refusals}
\alias{fb_refusals}
\title{List flexyBayes refusal reasons}
\usage{
fb_refusals(reason_code = NULL, since_version = NULL)
}
\arguments{
\item{reason_code}{Optional character vector of exact reason codes
to filter to. \code{NULL} (default) returns all registered reasons.}

\item{since_version}{Optional single version-string prefix to
filter to. \code{NULL} (default) returns all.}
}
\value{
A data frame of subclass \code{fb_refusals_table}, one row per
matching refusal reason, with columns \code{reason_code},
\code{description}, \code{since_version}, and \code{plan_field}. The print
method renders it as a compact checklist.
}
\description{
\code{fb_refusals()} exposes the locked refusal-reason registry as a
browsable table: the canonical vocabulary of conditions under which
flexyBayes declines to fit, route, or validate a model --- each
with a one-line description and the release it was introduced in.
It is the discovery surface for the structured refusals the package
raises. Every such refusal carries
a condition class \verb{flexybayes_refusal_<reason_code>}, so a reason
listed here can be caught precisely, for example with
\code{tryCatch(fit, flexybayes_refusal_precision_not_symmetric = handler)}.
}
\details{
Two optional filters narrow the listing. \code{reason_code} selects rows
by exact reason-code match (a single code or a vector). The
\code{since_version} filter selects reasons introduced in a matching
release by version-string prefix --- \code{since_version = "0.4"}
returns every reason added in the 0.4 series.

Routing-decision reasons (surfaced by \code{\link[=fb_plan]{fb_plan()}} and
\code{\link[=backend_decision]{backend_decision()}}) and internal control-flow signals are
deliberately excluded: this table lists only refusals a user can
actually encounter.
}
\examples{
fb_refusals()
fb_refusals(reason_code = "precision_not_symmetric")
fb_refusals(since_version = "0.4")

}
