% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_dirichlet.R
\name{fb_dirichlet}
\alias{fb_dirichlet}
\title{Fit a Dirichlet distribution to compositional data}
\usage{
fb_dirichlet(
  x,
  method = c("ml", "greta"),
  conf_level = 0.95,
  eps = 1e-06,
  n_samples = 2000L,
  warmup = 1000L,
  chains = 4L,
  verbose = FALSE
)
}
\arguments{
\item{x}{A numeric matrix or \code{data.frame} of compositional rows: at least
two columns, all entries non-negative, with at least four rows.}

\item{method}{Character. \code{"ml"} for maximum likelihood (the default) or
\code{"greta"} for the Bayesian route.}

\item{conf_level}{Numeric in \verb{(0, 1)}. The interval level for the parameter
summary. Defaults to \code{0.95}.}

\item{eps}{Numeric. The boundary nudge applied to exact zeros / ones.
Defaults to \code{1e-06}.}

\item{n_samples, warmup, chains}{Integer MCMC controls for the \code{"greta"}
method (ignored by \code{"ml"}). Default to \code{2000}, \code{1000}, and \code{4}.}

\item{verbose}{Logical. Whether the greta route prints sampler progress.
Defaults to \code{FALSE}.}
}
\value{
An object of class \code{c("fb_dirichlet_fit", "fb_family_fit")}: a
list with \code{estimates} (a \code{data.frame} of \code{term} / \code{estimate} /
\code{std.error} / \code{conf.low} / \code{conf.high}, one row per component),
\code{mean_composition} (numeric, summing to one), \code{method}, \code{n_obs},
\code{n_components}, \code{logLik} (ML only), and \code{draws} (greta only).
}
\description{
Estimates the concentration vector \eqn{\alpha} of a Dirichlet
distribution
from a matrix of compositional rows (each row a composition on the
simplex). The default method is maximum likelihood via base \code{optim()}
(dependency-free, deterministic); a Bayesian alternative uses greta's
native \code{dirichlet} distribution. Both routes return the same
\code{fb_dirichlet_fit} shape, so a \code{tidy()} summary is identical across
methods.
}
\details{
Rows are renormalised to sum to one, and exact zeros or ones are nudged
inside the open simplex by a small \code{eps} (a Dirichlet density is \code{-Inf} on
the simplex boundary). The maximum-likelihood route optimises the log
concentrations to keep them positive and recovers standard errors from the
Hessian by the delta method. The Bayesian route places independent
half-normal priors on the concentrations and reports posterior means with
quantile-based credible intervals.

The fitted mean composition (\code{alpha / sum(alpha)}) is reported on
\code{fit$mean_composition}.
}
\examples{
set.seed(1)
X <- rdirichlet(300L, alpha = c(2, 5, 3))
fit <- fb_dirichlet(X)
fit$estimates
}
\seealso{
\code{\link[=fb_family_dirichlet]{fb_family_dirichlet()}}, \code{\link[=rdirichlet]{rdirichlet()}}, \code{\link[=tidy.fb_dirichlet_fit]{tidy.fb_dirichlet_fit()}}
}
