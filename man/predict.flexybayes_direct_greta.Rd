% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.flexybayes_direct_greta}
\alias{predict.flexybayes_direct_greta}
\title{Predict from a flexybayes_direct_greta fit}
\usage{
\method{predict}{flexybayes_direct_greta}(
  object,
  newdata,
  predictor,
  type = c("response", "link"),
  n_draws = 500L,
  ...
)
}
\arguments{
\item{object}{A \code{flexybayes_direct_greta} object.}

\item{newdata}{A data frame.}

\item{predictor}{A function \verb{function(theta, newdata) -> numeric}
where \code{theta} is a named vector of canonical-named target
parameters and \code{newdata} is the data frame passed in.}

\item{type}{Character; \code{"response"} (default; on the response
scale) or \code{"link"} (on the linear-predictor scale). Currently
identity link only; non-Gaussian families queued for v0.3.}

\item{n_draws}{Integer; number of posterior draws to use (default
500). The predictor function is applied to each draw and the
mean is returned.}

\item{...}{Additional arguments (ignored).}
}
\value{
Numeric vector of length \code{nrow(newdata)}.
}
\description{
Direct-greta fits do not encode a Wilkinson-Rogers formula on the
IR, so \code{predict()} cannot mechanically apply the model matrix to
\code{newdata}. The user must supply an explicit predictor function
\code{f(theta, newdata)} mapping a length-\code{p} named parameter vector
and a data frame to a length-\code{nrow(newdata)} vector of predicted
values. Posterior-mean prediction is computed by applying \code{f()}
to each posterior draw and averaging.
}
