% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream_aggregate.R
\name{flexybayes_stream}
\alias{flexybayes_stream}
\title{Fit a mixed model to an out-of-core dataset by streaming aggregation}
\usage{
flexybayes_stream(
  fixed,
  random = NULL,
  source,
  family = "gaussian",
  trials = NULL,
  exposure = NULL,
  backend = c("inla", "greta"),
  chunk_rows = 5e+06,
  prior = NULL,
  fit = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{fixed}{A two-sided formula \code{response ~ fixed_terms}. The fixed
terms must be factors or factor interactions; a continuous term
forces one cell per row and is refused.}

\item{random}{A one-sided formula of random-intercept grouping factors,
ASReml style (for example \code{~ geno}), or \code{NULL} for no random terms.}

\item{source}{The data source. One of: a length-1 character path to an
\code{.fst} file; a length-1 character path to a delimited file readable
by \code{\link[data.table:fread]{data.table::fread()}}; an in-memory \code{data.frame} / \code{data.table}
(chunked internally, mainly for testing); or a function of one
argument \code{i} returning the \code{i}-th chunk as a \code{data.frame} and \code{NULL}
once the chunks are exhausted.}

\item{family}{A length-1 character family: \code{"gaussian"}, \code{"binomial"},
or \code{"poisson"}.}

\item{trials}{For \code{family = "binomial"}, the name of the trials-count
column, or \code{NULL} for Bernoulli (one trial per row).}

\item{exposure}{For \code{family = "poisson"}, the name of the exposure /
offset column, or \code{NULL} for unit exposure.}

\item{backend}{The estimation backend, \code{"inla"} (default) or
\code{"greta"}.}

\item{chunk_rows}{The number of rows to read per chunk. Larger chunks
read faster but use more peak memory; the default 5e6 keeps peak
memory modest while amortising read overhead.}

\item{prior}{An optional \code{\link[=fb_prior]{fb_prior()}} object. When \code{NULL} the backend
default priors are used.}

\item{fit}{When \code{TRUE} (default) the aggregated model is fitted and a
\verb{<flexybayes>} object is returned. When \code{FALSE} the function returns
the \verb{<fb_aggregated>} sufficient-statistics object without fitting --
useful for inspecting the compression a design will achieve.}

\item{verbose}{When \code{TRUE}, report streaming progress and the
compression achieved.}

\item{...}{Further arguments passed to the aggregated emit (for
example \code{n_samples}, \code{warmup}, \code{chains} for the greta backend).}
}
\value{
When \code{fit = TRUE}, a \verb{<flexybayes>} fit object carrying
\code{extras$aggregation_meta$streamed == TRUE}. When \code{fit = FALSE}, an
\verb{<fb_aggregated>} object.
}
\description{
\code{flexybayes_stream()} is the large-data entry point: it reads the data
a chunk at a time from \code{source}, accumulates additive sufficient
statistics per design cell, and fits the resulting aggregated model
through the same greta or INLA emit path as \code{\link[=flexybayes]{flexybayes()}}. The
aggregated likelihood is algebraically identical to the per-row
likelihood, so the posterior is the full-data posterior, not an
approximation.
}
\details{
Fit a cell-aggregatable mixed model to a dataset held on disk, reading
it in row-range chunks and accumulating exact per-cell sufficient
statistics rather than materialising the full table in memory.

The method scales by cell count \code{K} (the number of distinct factor
design combinations), not by row count \code{N}. A replicated factorial
design collapses billions of rows to a few thousand cells and fits in
seconds; a design that keys on a continuous covariate does not
compress and is refused before any chunk is read. Continuous fixed
effects, random slopes, structured covariance, and smooth terms break
the cell-constant linear-predictor property and are likewise refused
-- pass the data to \code{\link[=flexybayes]{flexybayes()}} per row for those models.

Supported families are \code{"gaussian"} (identity link), \code{"binomial"},
and \code{"poisson"}. For binomial data supply \code{trials} to name the column
of trial counts (a 0/1 response is treated as Bernoulli with one trial
per row). For poisson data supply \code{exposure} to name an exposure /
offset column.
}
\examples{
\dontshow{if (interactive() && requireNamespace("fst", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1L)
n <- 1e6
df <- data.frame(
  env = factor(sample(letters[1:6], n, replace = TRUE)),
  geno = factor(sample(1:50, n, replace = TRUE)),
  y = rnorm(n)
)
path <- tempfile(fileext = ".fst")
fst::write_fst(df, path)
fit <- flexybayes_stream(y ~ env, random = ~ geno, source = path,
                         backend = "inla")
summary(fit)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=flexybayes]{flexybayes()}} for the in-memory entry point;
\code{\link[=fb_prior]{fb_prior()}} for prior specification.
}
\author{
Max Moldovan, \email{max.moldovan@adelaide.edu.au}
}
