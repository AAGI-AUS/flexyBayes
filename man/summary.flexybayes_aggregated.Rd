% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.flexybayes_aggregated}
\alias{summary.flexybayes_aggregated}
\title{Summarise a fit run on the aggregated representation}
\usage{
\method{summary}{flexybayes_aggregated}(object, ...)
}
\arguments{
\item{object}{A \verb{<flexybayes_aggregated>} object, as returned by a fit
run on the aggregated representation.}

\item{...}{Ignored. Present for compatibility with the generic.}
}
\value{
Invisibly, an object of class
\code{c("summary.flexybayes", "list")} with the slots documented at
\code{\link[=summary.flexybayes]{summary.flexybayes()}}. Printed as a side effect.
}
\description{
Returns the same eleven-slot \code{summary.flexybayes} object every other
engine returns, so \code{summary(fit)$varcomp} answers on an aggregated fit
as it does on a per-row one. Aggregation is the default route for an
ordinary Gaussian call with a random term, so until 0.9.1 that slot
was \code{NULL} on the commonest fit the package produces: this method
returned its own list of the aggregated posterior's raw pieces and
carried no variance-component table at all.
}
\details{
Two things are aggregated-specific and both appear in the printed
header rather than as extra slots. The banner names the representation
(\code{aggregated-gaussian}, \code{aggregated-binomial}, \code{aggregated-poisson})
where a per-row fit names its engine, and the \code{aggregation} line gives
the row-to-cell compression the fit ran under. The \code{model} slot
carries the same compression in its text, because a reader comparing
the fixed-effect tables of an aggregated and a per-row fit otherwise
has nothing on the object saying the two were computed over different
numbers of rows.

The variance components come from the engine's own posterior. On the
INLA route that is the hyperparameter marginal transformed to the
standard-deviation scale, the same construction the per-row route
uses, and not the reciprocal square root of a tabulated precision
mean. A route that recorded posterior means alone reports the means
with the three interval columns \code{NA} and the row's \code{note} cell
reading \code{"no interval recorded"}.

The raw aggregated pieces this method used to return -- \code{beta_means},
\code{beta_vcov}, \code{sigma_means}, \code{tau_means} -- are unchanged at
\code{fit$extras$summary}.
}
\seealso{
\code{\link[=summary.flexybayes]{summary.flexybayes()}} for the slot-by-slot contract, and
\code{\link[=print.flexybayes_aggregated]{print.flexybayes_aggregated()}} for the one-screen fit description.
}
