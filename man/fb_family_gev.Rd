% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_extreme_value.R
\name{fb_family_gev}
\alias{fb_family_gev}
\title{Generalised extreme value (GEV) family object}
\usage{
fb_family_gev()
}
\value{
An object of class \code{c("fb_family_gev", "fb_family")}: a list with
\code{family} (the canonical string \code{"gen_extreme_value"}), \code{parameters}
(the character vector \code{c("location", "scale", "shape")}), \code{n_par} (the
integer \code{3}), \code{link} (\code{"identity"} on the location), and \code{fitter}
(\code{"fb_gev"}).
}
\description{
Constructs the family descriptor for the generalised extreme value
distribution, the limiting law of block maxima. The object mirrors the
shape of a base \code{stats::family()} descriptor -- a named list carrying the
family name, the parameter names, and the natural support -- so it sits
alongside the other flexyBayes families, while signalling that GEV is
fitted through the dedicated \code{fb_gev()} entry point rather than the
GLM-link emit path.
}
\details{
The GEV is parameterised by a location \eqn{\mu}, a positive scale
\eqn{\sigma}, and a shape \eqn{\xi} (the extreme value index). The shape
governs the tail: \eqn{\xi > 0} gives the heavy-tailed Frechet type,
\eqn{\xi < 0} the bounded Weibull type, and \eqn{\xi \to 0} the Gumbel
limit.
}
\examples{
fam <- fb_family_gev()
fam$parameters
}
\seealso{
\code{\link[=fb_gev]{fb_gev()}}
}
