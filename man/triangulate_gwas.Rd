% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate_genomic.R
\name{triangulate_gwas}
\alias{triangulate_gwas}
\title{Triangulate two genome-wide association scans}
\usage{
triangulate_gwas(a, b, alpha = 0.05, top_k = 10L)
}
\arguments{
\item{a, b}{An \code{fb_gwas} object, or a GWAS lens
\verb{list(results = <data frame with marker, p_value, effect>, lambda_gc)}.}

\item{alpha}{Genome-wide significance threshold on the Bonferroni-
adjusted p-value for the hit-set comparison (default 0.05).}

\item{top_k}{Size of the top-marker overlap comparison (default 10).}
}
\value{
A \code{triangulate_gwas_result}: \code{jaccard} (significant-set
overlap), \code{n_sig_a} / \code{n_sig_b} / \code{n_sig_common}, \code{top_k_overlap},
\code{effect_correlation} (on common markers), and \code{lambda_gc_a} /
\code{lambda_gc_b}.
}
\description{
Compare two GWAS results -- typically a flexyBayes \code{\link[=fb_gwas]{fb_gwas()}} scan
against a field-standard scan (GEMMA / rrBLUP, supplied through the
koine oracle) -- by the agreement that matters for a scan: do the same
loci come up? Reports the Jaccard overlap of the genome-wide-significant
marker sets, the overlap among the top markers, the correlation of the
marker effects on the markers in common, and each scan's genomic-control
inflation factor.
}
\examples{
mk <- function(sig) data.frame(
  marker = paste0("snp", 1:6),
  p_value = ifelse(seq_len(6) \%in\% sig, 1e-9, 0.5),
  p_bonferroni = ifelse(seq_len(6) \%in\% sig, 6e-9, 1),
  effect = c(2, 0, 0, -1.5, 0, 0)
)
a <- list(results = mk(c(1, 4)), lambda_gc = 1.01)
b <- list(results = mk(c(1, 4)), lambda_gc = 0.99)
triangulate_gwas(a, b)
}
\seealso{
\code{\link[=fb_gwas]{fb_gwas()}}, \code{\link[=triangulate_genomic]{triangulate_genomic()}}.
}
