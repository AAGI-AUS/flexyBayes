% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_draws_support.R
\name{as_draws_df.flexybayes}
\alias{as_draws_df.flexybayes}
\alias{as_draws.flexybayes}
\alias{as_draws_matrix.flexybayes}
\title{Posterior draws from a flexyBayes fit}
\usage{
\method{as_draws_df}{flexybayes}(x, n_draws = 1000L, ...)

\method{as_draws}{flexybayes}(x, n_draws = 1000L, ...)

\method{as_draws_matrix}{flexybayes}(x, n_draws = 1000L, ...)
}
\arguments{
\item{x}{A fitted \code{flexybayes} object from either active engine.}

\item{n_draws}{Number of posterior draws to return, matching the
default of \code{\link[=fb_as_draws_simple]{fb_as_draws_simple()}}. Used on the INLA path, where the
draws are sampled from the fitted approximation; ignored on the brms
path, which returns the draws the sampler kept.}

\item{...}{Passed to the underlying draws extraction.}
}
\value{
A \pkg{posterior} draws object holding one column per
canonical parameter: a \code{draws_df} from \code{as_draws_df()} and from
\code{as_draws()}, a \code{draws_matrix} from \code{as_draws_matrix()}.
}
\description{
Methods for the \pkg{posterior} package's \code{as_draws()},
\code{as_draws_df()} and \code{as_draws_matrix()} generics, so a fitted model
hands its posterior to the Bayesian workflow ecosystem --
\pkg{posterior} summaries, \pkg{bayesplot} displays, anything reading
a \code{draws} object -- in the shape those tools expect.
}
\details{
The parameter names are canonical and the same on every engine:
\code{(Intercept)}, one entry per fixed-effect term, \code{sigma} for the
residual standard deviation, and \verb{sd_<group>} for the
standard deviation of each random-effect group. Variance components
are on the \strong{standard-deviation scale} whatever the engine stored:
INLA's precision hyperparameters are transformed on the way out, so
\code{sd_g} is a draw of a standard deviation and not of a precision. This
is the same view \code{\link[=triangulate]{triangulate()}} compares, and it is built by the same
internal seam, so a name in one is a name in the other.
}
\section{Where the draws come from}{


On a \strong{brms} fit the draws are the sampler's own: the kept
post-warmup iterations, renamed. \code{n_draws} is ignored, because the
number of draws was fixed when the model was sampled.

On an \strong{INLA} fit there is no sampler. The fit is a nested Laplace
approximation, and the draws are sampled \emph{from that fitted
approximation} by \code{INLA::inla.posterior.sample()}, reading the
configuration store every flexyBayes INLA fit is built with
(\code{control.compute = list(config = TRUE)}). Two consequences follow,
and they are the ones Tutorials 01 and 10 teach:

\itemize{
\item The \strong{fitting} is deterministic. The Laplace approximation
draws no random numbers, so a \code{seed} passed to \code{\link[=flexybayes]{flexybayes()}}
changes nothing about the fit itself.
\item The \strong{sampling} step is not, and it is not reproducible from
\code{set.seed()} alone. The hyperparameter draws follow R's random
stream, but the latent field -- the intercept and the
fixed-effect terms -- is drawn by INLA's own generator, which
\code{INLA::inla.posterior.sample()} seeds at random unless its own
\code{seed} argument is given, and flexyBayes leaves that argument at
its default. \code{?INLA::inla.posterior.sample} states that
reproducing a sample needs that seed \emph{and} R's RNG state fixed.
Treat the result as one sample of the posterior rather than a
fixed object: Monte-Carlo error in anything computed from it
shrinks with \code{n_draws}, so raise \code{n_draws} before reading a small
difference between two sets of draws as a difference between two
posteriors.
}
}

\seealso{
\code{\link[=fb_as_draws_simple]{fb_as_draws_simple()}} for the same draws as a plain named
list, with no \pkg{posterior} dependency; \code{\link[=canonical_names]{canonical_names()}} for
the name map itself; \code{\link[=prior_summary]{prior_summary()}} for the priors those draws
were taken under; \code{\link[=summary.flexybayes]{summary.flexybayes()}}, whose \verb{$converge} slot
carries the engine's own convergence diagnostics; \code{\link[=triangulate]{triangulate()}}
for a two-engine comparison over the same canonical view.
}
