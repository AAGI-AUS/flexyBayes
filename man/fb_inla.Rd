% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_inla.R
\name{fb_inla}
\alias{fb_inla}
\title{Fit a flexyBayes model via the INLA engine}
\usage{
fb_inla(...)
}
\arguments{
\item{...}{Arguments passed to \code{\link[=flexybayes]{flexybayes()}} (e.g. \code{fixed}, \code{random},
\code{rcov}, \code{data}, \code{family}, \code{prior}, \code{syntax}). The \code{backend} argument
is pinned to \code{"inla"} and must not be supplied.}
}
\value{
An object of class \code{"flexybayes"} (specifically a
\code{flexybayes_inla} fit); see \code{\link[=flexybayes]{flexybayes()}} for the structure.
}
\description{
Engine pin: fits the model with INLA (integrated nested Laplace
approximation) only. This is sugar for
\code{\link[=flexybayes]{flexybayes()}}\verb{(..., backend = "inla")} and accepts the same arguments
and grammars (an ASReml \code{fixed} / \code{random} / \code{rcov} specification or a
brms-style bar-grouped formula -- see \code{\link[=flexybayes]{flexybayes()}} for the full
argument list). The model must be latent-Gaussian feasible; if it is
not, the shared \code{lgm_gate()} raises a structured refusal naming the
offending term, exactly as \code{flexybayes(backend = "inla")} does.
}
\details{
Sampling-control arguments (\code{n_samples}, \code{warmup}, \code{chains},
\code{mcmc_verbose}) are accepted for call-compatibility with the other
engine pins but are inert under INLA's deterministic Laplace
approximation.
}
\examples{
df <- data.frame(
  yield = rnorm(40),
  geno  = factor(rep(letters[1:8], 5)),
  env   = factor(rep(c("a", "b"), 20))
)
\donttest{
if (requireNamespace("INLA", quietly = TRUE)) {
  fit <- fb_inla(yield ~ env, random = ~ geno, data = df)
}
}
}
\seealso{
\code{\link[=flexybayes]{flexybayes()}} and \code{\link[=fb]{fb()}} for the universal entry that picks a
backend; \code{\link[=fb_from_asreml]{fb_from_asreml()}} / \code{\link[=fb_from_brms]{fb_from_brms()}} for the ingest layer.

Other flexyBayes engine pins: 
\code{\link{fb_brms}()},
\code{\link{fb_greta}()}
}
\concept{flexyBayes engine pins}
