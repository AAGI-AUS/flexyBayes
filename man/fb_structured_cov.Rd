% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/structured_cov_report.R
\name{fb_structured_cov}
\alias{fb_structured_cov}
\title{Identified covariance for factor-analytic structured-covariance terms}
\usage{
fb_structured_cov(fit)
}
\arguments{
\item{fit}{A \code{flexybayes} fit produced on the greta backend with at
least one \code{fa()} structured-covariance term.}
}
\value{
A named list with one entry per factor-analytic term (named by
the term's outer factor). Each entry is a list with: \code{levels} (the
outer-factor levels labelling the rows/columns), \code{cov_mean},
\code{cov_lower}, \code{cov_upper} (posterior mean and 95\% interval of
\eqn{G}), \code{cor_mean} (posterior-mean correlation), \code{rhat} (entrywise
Rhat of \eqn{G}, \code{NA} if fewer than two chains), \code{max_rhat}, and
\code{k} (the number of factors). Returns an empty list (with a message)
when the fit carries no factor-analytic term. Non-factor-analytic
structured terms (\code{us}, \code{ar1}) are reported as not-yet-reconstructed.
}
\description{
For each \code{fa(outer, k)} term in a greta fit, reconstruct the implied
outer-factor covariance \eqn{G = \Lambda\Lambda^\top + \mathrm{diag}(\psi)}
from the posterior draws and summarise it. Unlike the raw loadings
\eqn{\Lambda} -- which are identified only up to rotation and sign, so
their per-entry Rhat is meaningless -- the covariance \eqn{G} and the
correlation derived from it are rotation- and sign-invariant. They are
therefore the identified quantities whose posterior is interpretable
and whose Rhat is a genuine convergence diagnostic. Consult this in
preference to the raw-loading Rhat when judging whether a
factor-analytic fit has converged.
}
\examples{
\dontrun{
fit <- flexybayes(
  y ~ 1, random = ~ fa(env, 2):id(geno), data = met_data,
  family = "gaussian", backend = "greta"
)
sc <- fb_structured_cov(fit)
sc$env$cov_mean   # identified genetic covariance across environments
sc$env$max_rhat   # convergence of the identified quantity
}

}
