% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/triangulate_genomic.R
\name{triangulate_genomic}
\alias{triangulate_genomic}
\title{Triangulate genomic model outputs across engines or against a field lens}
\usage{
triangulate_genomic(a, b, term = NULL, data_independence = NA)
}
\arguments{
\item{a, b}{A flexyBayes GBLUP / pedigree fit, or a genomic-lens list. A
lens entry for \code{h2} / \code{var_g} / \code{var_e} may be a numeric vector of
draws or \code{list(estimate, se)} (a REML point); \code{gebv} is a named
numeric vector keyed by genotype.}

\item{term}{Optional grouping-factor name selecting the relationship
term when a fit has more than one.}

\item{data_independence}{\code{TRUE} if the two lenses used independently
sourced data (suppresses the caveat); \code{NA} (default) or \code{FALSE}
attaches it.}
}
\value{
A \code{triangulate_genomic_result}: \code{components} (a data frame
comparing heritability and the variance components -- value and
interval per lens, the difference, and an interval-overlap flag),
\code{gebv} (the Pearson / Spearman correlation of the matched breeding
values and the number in common), the lens labels, and the caveat.
}
\description{
Compare two genomic analyses -- heritability, variance components, and
genomic estimated breeding values -- on a common footing. Each argument
is either a flexyBayes GBLUP / pedigree fit (greta, INLA, or brms) or a
generic \emph{genomic lens} (\code{list(h2, var_g, var_e, gebv, label)}), the form
a field-standard oracle such as sommer's REML supplies. Cross-engine use
checks that the Bayesian backends agree; the lens form lets the koine
fourth opinion (REML / established tools) cross-check the Bayesian
answer -- the orchestra's signature value in a field with decades of
established methods.
}
\details{
Like \code{\link[=triangulate]{triangulate()}}, this measures inter-lens \emph{agreement}, not
correspondence: two lenses fit to the same data share any fabricated
upstream data fact, so the result carries a shared-upstream caveat
unless \code{data_independence = TRUE}.
}
\examples{
# A Bayesian posterior (draws) against a field-standard REML point lens.
set.seed(1)
bv <- c(2, 1, 0, -1, -2)
bayes <- list(
  h2 = rnorm(500, 0.5, 0.05),
  gebv = stats::setNames(bv + rnorm(5, 0, 0.2), paste0("g", 1:5))
)
reml <- list(
  h2 = list(estimate = 0.48, se = 0.04),
  gebv = stats::setNames(bv + rnorm(5, 0, 0.2), paste0("g", 1:5))
)
triangulate_genomic(bayes, reml)
}
\seealso{
\code{\link[=triangulate]{triangulate()}}, \code{\link[=genomic_summary]{genomic_summary()}}, \code{\link[=triangulate_gwas]{triangulate_gwas()}}.
}
