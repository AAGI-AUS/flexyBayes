% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fb_from_brms.R
\name{fb_from_brms}
\alias{fb_from_brms}
\title{Ingest a brms-format formula into the flexyBayes IR}
\usage{
fb_from_brms(
  formula,
  data,
  family = "gaussian",
  link = NULL,
  prior = NULL,
  weights = NULL,
  prior_fixed_sd = 100,
  prior_vc_sd = 1,
  carry_n_rows = NULL,
  ...
)
}
\arguments{
\item{formula}{A base-R formula (recommended) or a \code{brmsformula}
object. \code{brmsformula} support requires \pkg{brms} to be installed.}

\item{data}{A data.frame containing every referenced variable. May be
\code{NULL} only on the advanced metadata-only path (see \code{carry_n_rows}).}

\item{family}{Character family name or a base-R \code{family()} object.}

\item{link}{Character link override, or \code{NULL} for the family default.}

\item{prior}{An \code{\link[=fb_prior]{fb_prior()}} object, or \code{NULL}.}

\item{weights}{Optional numeric weights vector of length \code{nrow(data)},
mapped to a single addition term with \code{type = "weights"}.}

\item{prior_fixed_sd}{Numeric SD for fixed-effect normal priors when
\code{prior} is \code{NULL}.}

\item{prior_vc_sd}{Numeric hyperparameter for the variance-component
priors when \code{prior} is \code{NULL}.}

\item{carry_n_rows}{Advanced: a positive integer enabling the
metadata-only IR path (\code{data = NULL}). The formula's variables are
realised as a one-row placeholder and the row count is recorded as
\code{carry_n_rows}, for stress-testing the preflight layer without
materialising the full data. Leave \code{NULL} for ordinary use.}

\item{...}{Reserved for future brms-ingest options (specials, autocor
handling); currently unused.}
}
\value{
An \code{fb_terms} object with \code{source = "brms"}.
}
\description{
Parses a brms / lme4-style two-sided formula
(\code{response ~ fixed + (1 | g)}) into a \code{fb_terms} object --
flexyBayes's backend-agnostic intermediate representation (IR). The IR
is what every engine emits from, so building it explicitly lets a
power user inspect the parsed model, cache it, or hand it to a fitting
verb. Most users never call this directly: \code{\link[=fb]{fb()}} and \code{\link[=flexybayes]{flexybayes()}}
detect a brms-style \code{formula} argument and build the IR internally.
}
\details{
Linear fixed effects, random intercepts (\code{(1 | g)}, \code{(1 || g)}) and
uncorrelated random intercept+slope (\code{(x || g)}) are supported;
correlated random slopes (\code{(x | g)}), smoothers, Gaussian processes
and autocorrelation terms refuse at ingest with a structured message.
}
\examples{
df <- data.frame(
  y = rnorm(30),
  x = rnorm(30),
  g = factor(rep(letters[1:5], 6))
)
ir <- fb_from_brms(y ~ x + (1 | g), data = df)
class(ir)
}
\seealso{
\code{\link[=fb]{fb()}} and \code{\link[=flexybayes]{flexybayes()}} for the universal fitting entry;
\code{\link[=fb_from_asreml]{fb_from_asreml()}} and \code{\link[=fb_from_greta]{fb_from_greta()}} for the other dialects.

Other flexyBayes ingest adapters: 
\code{\link{fb_from_asreml}()},
\code{\link{fb_from_greta}()}
}
\concept{flexyBayes ingest adapters}
