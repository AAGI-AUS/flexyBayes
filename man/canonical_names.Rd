% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_names.R
\name{canonical_names}
\alias{canonical_names}
\alias{canonical_names.flexybayes_inla}
\alias{canonical_names.flexybayes_brms}
\title{Canonical parameter-name view for a flexyBayes fit}
\usage{
canonical_names(fit, drop = FALSE, ...)

\method{canonical_names}{flexybayes_inla}(fit, drop = FALSE, ...)

\method{canonical_names}{flexybayes_brms}(fit, drop = FALSE, ...)
}
\arguments{
\item{fit}{A \code{flexybayes_inla} or \code{flexybayes_brms} object.}

\item{drop}{Logical: if \code{TRUE} (default \code{FALSE}), drop
backend-native names that are not in the registered map
(e.g., INLA's \verb{Predictor.<i>} latent-predictor draws). When
\code{FALSE}, un-mapped names appear in the returned \verb{$unmapped}
element.}

\item{...}{Additional arguments (ignored by current methods).}
}
\value{
A list holding the canonical-name map and its provenance.

\describe{
\item{\code{map}}{Named character vector keyed by backend-native
parameter name with canonical name as the value.}
\item{\code{transform}}{Named list of \verb{function(x) -> x'}
transforms keyed by canonical name. Empty list when no
transforms apply.}
\item{\code{source}}{Character: \code{"registry"}, \code{"user"},
\code{"registry_fallback_verbatim"}, or \code{"legacy_inferred"}.}
\item{\code{unmapped}}{Character vector of backend-native names
not in the map (when \code{drop = FALSE}).}
\item{\code{prior_parametrization}}{Character, present only on
aggregated fits, naming which prior the fit ran under.
\code{"per_row_equivalent"} is the legacy scalar bridge, whose
precision prior makes the aggregated posterior match the
per-row posterior to numerical precision.
\code{"package_default"} is the automatic bounded-uniform-on-SD
prior, which claims no such equivalence on this route.
\code{"custom"} is an explicit prior from the caller (see the
"Matched priors" note on \code{\link[=triangulate]{triangulate()}}).}
}
}
\description{
Returns the backend-native -> canonical parameter-name map for a
fit, plus per-parameter value transforms where applicable (e.g.,
the INLA precision-to-SD \code{sqrt(1/prec)} transform applied to
hyperparameters before triangulation). The canonical convention
follows brms (\code{(Intercept)}, \verb{<term>}, \verb{sd_<group>}, \code{sigma},
\verb{r_<group>[<level>]}).
}
\details{
On \code{flexybayes_inla} and \code{flexybayes_brms} fits, the per-backend
mapper registered at package load (\code{inla} or \code{brms}) drives the
resolution; the returned map is cached on
\code{fit$extras$canonical_map} for fast repeated access.
}
