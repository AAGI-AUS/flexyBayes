# Update a flexybayes model

Re-fit the model with modified arguments.

## Usage

``` r
# S3 method for class 'flexybayes'
update(object, ...)
```

## Arguments

- object:

  A flexybayes object

- ...:

  Arguments to override (e.g., `n_samples = 2000`)

## Value

A new flexybayes object
