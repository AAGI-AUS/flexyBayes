# Test whether an object is an `fb_engine` specification

Test whether an object is an `fb_engine` specification

## Usage

``` r
is_fb_engine(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is an `fb_engine` object.
