# Extract fitted values

Extract fitted values

## Usage

``` r
# S3 method for class 'flexybayes'
fitted(object, ...)
```

## Arguments

- object:

  A flexybayes object

- ...:

  Additional arguments (ignored)

## Value

Numeric vector of posterior mean fitted values
