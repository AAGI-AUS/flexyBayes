# Test whether an object is an `fb_cov` carrier

Test whether an object is an `fb_cov` carrier

## Usage

``` r
is_fb_cov(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is an `fb_cov` object.
