# Print an `<fb_plan>` — flight-checklist form

Print an `<fb_plan>` — flight-checklist form

## Usage

``` r
# S3 method for class 'fb_plan'
print(x, ...)
```

## Arguments

- x:

  an `<fb_plan>` object.

- ...:

  unused.
