# Summarise an `<fb_plan>` — verbose form

Summarise an `<fb_plan>` — verbose form

## Usage

``` r
# S3 method for class 'fb_plan'
summary(object, ...)
```

## Arguments

- object:

  an `<fb_plan>` object.

- ...:

  unused.
