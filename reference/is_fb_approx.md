# Test whether an object is an `fb_approx` specification

Test whether an object is an `fb_approx` specification

## Usage

``` r
is_fb_approx(x)
```

## Arguments

- x:

  An object.

## Value

`TRUE` if `x` is an `fb_approx` object.
