# Print method for triangulate_result

Internal S3 method. Brief summary header followed by the per- parameter
metrics table.

## Usage

``` r
# S3 method for class 'triangulate_result'
print(x, ...)
```

## Arguments

- x:

  A `triangulate_result` object as returned by
  [`triangulate()`](https://aagi-aus.github.io/flexyBayes/reference/triangulate.md).

- ...:

  Ignored. Present for compatibility with the generic.

## Value

Invisibly, `x` unchanged. Called for the header and metrics table it
prints.
