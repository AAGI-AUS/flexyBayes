# Print an `fb_engine` specification

Print an `fb_engine` specification

## Usage

``` r
# S3 method for class 'fb_engine'
print(x, ...)
```

## Arguments

- x:

  An `fb_engine` object.

- ...:

  Unused.

## Value

`x`, invisibly.
