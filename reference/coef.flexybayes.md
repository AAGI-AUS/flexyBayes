# Extract fixed effect coefficients

Extract fixed effect coefficients

## Usage

``` r
# S3 method for class 'flexybayes'
coef(object, ...)
```

## Arguments

- object:

  A flexybayes object

- ...:

  Additional arguments (ignored)

## Value

Named numeric vector of posterior mean fixed effects
